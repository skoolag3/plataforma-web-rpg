# URL interna usada pelo proxy /api durante o build e a execucao na Vercel.
BACKEND_URL=http://localhost:3001

# Mantenha /api para o navegador acessar o backend pelo mesmo dominio do frontend.
NEXT_PUBLIC_API_URL=/api
